<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.MemberContentsDAO">
	<insert id="memberContents_add" parameterType="vo.MemberContentsVO">
		insert into memberContents_tb (memberContents_memberId, memberContents_docId, memberContents_portNum)
		values (#{memberId}, #{docId}, #{portNum})
	</insert>
	
	<update id="memberContents_modify" parameterType="vo.MemberContentsVO">
		update memberContents_tb
		set memberContents_memberId=#{memberId}, memberContents_portNum=#{portNum}
		where memberContents_docId=#{docId}
	</update>
	
	<select id="memberContents_search" parameterType="vo.MemberContentsVO" resultType="vo.MemberContentsVO">
		select memberContents_memberId memberId, memberContents_docId docId, memberContents_portNum portNum from memberContents_tb 
		where memberContents_docId=#{docId}
	</select>

	<select id="memberContents_searchAll" resultType="vo.MemberContentsVO">
		select memberContents_memberId memberId, memberContents_docId docId, memberContents_portNum portNum from memberContents_tb from memberContents_tb
		where memberContents_memberId=#{memberId}
	</select>
	
	<delete id="memberContents_delete" parameterType="vo.MemberContentsVO">
        delete from memberContents_tb where memberContents_docId=#{docId}
    </delete>
</mapper>