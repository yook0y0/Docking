<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.EditorReviewDAO">
	<insert id="editorReview_add" parameterType="vo.EditorReviewVO">
		insert into
		editorReview_tb (editorReview_reviewId, editorReview_editor, editorReview_writer, editorReview_review, editorReview_score)
		values
		(#{reviewId}, #{editor}, #{writer}, #{review}, #{score})
	</insert>

	<update id="editorReview_modify" parameterType="vo.EditorReviewVO">
		update
		editorReview_tb
		set editorReview_reviewId=#{reviewId}, editorReview_editor=#{editor}, editorReview_writer=#{writer}, editorReview_review=#{review}, editorReview_score=#{score}
		where editorReview_reviewId=#{reviewId}
	</update>

	<select id="editorReview_search" parameterType="vo.EditorReviewVO"
		resultType="vo.EditorReviewVO">
		select editorReview_reviewId reviewId, editorReview_editor editor, editorReview_writer writer, editorReview_review review, editorReview_score score from editorReview_tb
		where editor_name=#{editorName}
	</select>

	<select id="editorReview_searchAll" resultType="vo.EditorReviewVO">
		select editorReview_reviewId reviewId, editorReview_editor editor, editorReview_writer writer, editorReview_review review, editorReview_score score from editorReview_tb
		where editorReview_editor=#{editor}
	</select>

	<delete id="editorReview_delete" parameterType="vo.EditorReviewVO">
		delete from editorReview_tb where editorReview_reviewId=#{reviewId}
	</delete>
</mapper>