<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.EditorCodeDAO">
	<insert id="editorCode_add" parameterType="vo.EditorCodeVO">
		insert into
		editorCode_tb (editorCode_editor, editorCode_body, editorCode_path)
		values
		(#{editor}, #{body}, #{path})
	</insert>

	<update id="editorCode_modify" parameterType="vo.EditorCodeVO">
		update
		editorCode_tb
		set editorCode_editor=#{editor}, editorCode_body=#{body}, editorCode_path=#{path}
		where editorCode_path=#{path}
	</update>

	<select id="editorCode_search" parameterType="vo.EditorCodeVO"
		resultType="vo.EditorCodeVO">
		select editorCode_editor editor, editorCode_body body, editorCode_path path from editorCode_tb
		where editorCode_path=#{path}
	</select>

	<select id="editorCode_searchAll" resultType="vo.EditorCodeVO">
		select editorCode_editor editor, editorCode_body body, editorCode_path path from editorCode_tb
		where editorCode_editor=#{editor}
	</select>

	<delete id="editorCode_delete" parameterType="vo.EditorCodeVO">
		delete from editorCode_tb where editorCode_path=#{path}
	</delete>
</mapper>