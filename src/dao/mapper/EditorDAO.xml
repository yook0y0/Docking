<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.EditorDAO">
	<insert id="editor_add" parameterType="vo.EditorVO">
		insert into
		editor_tb (editor_director, editor_name, editor_info, editor_startPage, editor_getMethod, editor_setMethod)
		values
		(#{director}, #{name}, #{info}, #{startPage}, #{getMethod}, #{setMethod})
	</insert>

	<update id="editor_modify" parameterType="vo.EditorVO">
		update
		editor_tb
		set editor_director=#{director}, editor_name=#{name}, editor_info=#{info}, editor_startPage=#{startPage}, editor_getMethod=#{getMethod}, editor_setMethod=#{setMethod}
		where editor_name=#{name}
	</update>

	<select id="editor_search" parameterType="vo.EditorVO"
		resultType="vo.EditorVO">
		select editor_director director, editor_name name, editor_info info, editor_startPage startPage, editor_getMethod getMethod, editor_setMethod setMethod from editor_tb
		where editor_name=#{editorName}
	</select>

	<select id="editor_searchAll" resultType="vo.EditorVO">
		select editor_director director, editor_name name, editor_info info, editor_startPage startPage, editor_getMethod getMethod, editor_setMethod setMethod from editor_tb
		where editor_director=#{director}
	</select>

	<delete id="editor_delete" parameterType="vo.EditorVO">
		delete from editor_tb where editor_name=#{name}
	</delete>
</mapper>