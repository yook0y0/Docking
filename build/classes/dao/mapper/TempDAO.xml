<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.TempDAO">
	<insert id="temp_add" parameterType="vo.temp.TempVO">
		insert into temp_tb (temp_date, temp_editor, temp_contents)
		values (#{date}, #{editor}, #{contents})
	</insert>
	
	<update id="temp_modify" parameterType="vo.temp.TempVO">
		update temp_tb
		set temp_editor=#{editor}, temp_contents=#{contents}
		where temp_date=#{date}
	</update>
	
	<select id="temp_search" parameterType="vo.temp.TempVO" resultType="vo.temp.TempVO">
		select temp_date date, temp_editor editor, temp_contents contents from temp_tb 
		where temp_date=#{date}
	</select>

	<select id="temp_searchAll" resultType="vo.temp.TempVO">
		select temp_date date, temp_editor editor, temp_contents contents from temp_tb
	</select>
	
	<delete id="temp_delete" parameterType="vo.temp.TempVO">
        delete from temp_tb where temp_date=#{date}
    </delete>
</mapper>